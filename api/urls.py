from django.urls import path
from . import views

urlpatterns = [
    # path('products/<pk>/', views.ProductView.as_view(), name='product-view'),
    # path('order/<pk>/', views.OrdserDetailView.as_view(), name='order-detail-view'),
    # path('order/', views.OrderCreateView.as_view(), name='order-create-view'),
]
